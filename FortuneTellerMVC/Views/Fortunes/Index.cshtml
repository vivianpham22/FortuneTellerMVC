@model IEnumerable<FortuneTellerMVC.Models.Fortune>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customers.Firstame)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customers.Firstame)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FortuneID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FortuneID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FortuneID })
        </td>
    </tr>
}

</table>
